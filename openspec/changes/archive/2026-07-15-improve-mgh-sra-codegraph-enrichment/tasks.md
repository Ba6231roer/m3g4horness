## 1. Shared codegraph-hint fragment + stage prompt wiring

- [x] 1.1 Verify `core/prompts/fragments/codegraph-hint.md` exists (created by sibling change `improve-mgh-init-codegraph-enrichment`, task 1.1 already `[x]`). It holds generic steering: `codegraph=on` → SHALL prefer MCP `codegraph_explore` / CLI `codegraph explore` fallback → Read only for uncovered items (non-indexed lang / >`--big-file-bytes` / index-missing / `⚠️ pending sync` banner files), 主谓 non-「you may」. If absent (SRA applied before init), create it byte-identical to the init change's spec. **Do NOT add SRA-specific content here** (D8: SRA semantics live in the stage stanza, not the fragment).
- [x] 1.2 `core/prompts/stages/sra-augment.md`: add a `codegraph=on` stanza referencing the fragment (Sanctioned-tools allowlist gains `codegraph_explore`/`codegraph explore` when signaled). Add the **call-path confirmation** sub-task (see §2). Keep `codegraph=off` behavior byte-identical. Add purity note: `codegraph` as operational external-tool reference permitted in structural fields; human-readable `risk`/`reason`/`note` SHALL NOT carry tool-internal prose.
- [x] 1.3 `core/prompts/stages/sra-clarify.md`: add a `codegraph=on` stanza referencing the fragment (advisory callers pre-resolution per O4: only reduce a role/ownership question when a codegraph caller maps to a known `memory.roles[]` role; never override user/code). Keep `codegraph=off` byte-identical.
- [x] 1.4 `core/prompts/stages/sra-consistency.md`: note that a4 SHALL **pass through + normalize** `recommended_control.call_path` wording across caps (same control referenced in multiple caps → consistent `call_path` phrasing) but MUST NOT recompute it (no codegraph in a4).

## 2. a3 call-path confirmation signal (the headline) + draft contract

- [x] 2.1 `core/prompts/stages/sra-augment.md` (or its `codegraph=on` stanza from 1.2): after Task 2 (three-signal match), apply **codegraph structural-evidence confirmation** (4 facets, same mechanism): **(1) call-path [primary, structured field]** — when a gap has a `recommended_control`, resolve via `codegraph_explore` call-path / `callers` + framework routes whether the control is wired onto the gap endpoint's **request path** (entry → protected resource); emit advisory `call_path: {confirmed: true|false|null, path: [{file,line,edge}, ...], source: "codegraph", note: "<简体中文>"}` (`true` → strengthen reuse wording; `false` → downgrade + note "控制存在但未确认接入此接口"; `null` → ceiling/bounded-trim). **(2) data-flow [advisory, `callees`]** — is the gap's sensitive field actually flowed/returned/logged by the endpoint? Sharpen or drop false sensitive-data/injection gaps (improve `risk`/anchor, NO new field). **(3) liveness [advisory, `callers`/blast radius]** — does the recommended control have callers or is it near-dead-code? Note in `call_path.note` (strengthens existence≠effectiveness). **(4) domain-sibling [advisory, `callers`/`callees` clustering]** — enumerate same-domain sibling endpoints + their guards; strengthen signal-2 `reason` beyond textual path-similarity (NO new field, NO change to three-signal hit criteria). Hard rules: ground every structural claim in codegraph-returned real `file:line`; `confirmed` MUST NOT be fabricated; facets 2–4 improve existing `evidence`/`risk`/`reason` prose only, NO new draft schema; advisory MUST NOT override code evidence / user `business_context.json` assertion. Sanctioned-tools: codegraph MCP/CLI + Read fallback; NEVER `Write .py` / `py -c` / direct `specs/`/`tasks.md` edit; `draft_path` verbatim.
- [x] 2.2 **Bounded + fail-soft** clause in the stanza: only resolve for **already-recommended controls / anchored gaps** (not all candidates); call-path is the primary facet done first; facets 2–4 done only when budget allows and trimmed first under pressure. When (gap×control×facet) exceed the single a3 context budget, resolve **top-1 control's call-path per gap** (by `matched_signals` strength + evidence confidence), mark the rest `confirmed:null`, annotate the draft "部分未确认". `codegraph=off` / no `--rules` (no recommendation) → skip entirely, no `call_path` field, no advisory enrichment.
- [x] 2.3 Mirror the `codegraph=on` stanza + call-path hard constraints into claude agent def `releases/claude-code/agents/sra-augment.md` (NEVER `Write .py`/`py -c`; `draft_path` verbatim; call_path advisory-only).
- [x] 2.4 Mirror byte-parity into opencode agent def `releases/opencode/agent/sra-augment.md` (opencode YAML frontmatter). Also mirror the lighter `codegraph=on` stanza (advisory callers) into both `sra-clarify` agent defs (claude + opencode); mirror the pass-through note into both `sra-consistency` agent defs.
- [x] 2.5 Extend draft contract `core/contracts/sra/augmentation.md`: document optional `gaps[].recommended_control.call_path` (`{confirmed,path[],source:"codegraph",note}`; `confirmed:null` = unconfirmed/trimmed); note it is advisory, render-time only (a5 wording/confidence, no managed-block structural change), and that its absence (`codegraph=off`) is valid.

## 3. Command shells (both claude + opencode) — detection, flow, flag, map, disclosure

- [x] 3.1 Add detection stanza to both `mgh-sra.md` (claude `commands/` + opencode `command/`): after a1 prepare, `test -d <MGH_TARGET>/.codegraph && command -v codegraph` → `codegraph=on|off` signal; `<MGH_TARGET>` = a1 stdout `project_root`; set BEFORE spawning any LLM subagent; pass verbatim into a2/a3 subagent task inputs. Zero LLM token for detection.
- [x] 3.2 Add `--no-codegraph` to the Parse-arguments flag table of both shells (default `auto`; opt-out aligns with `--rules`/`--skip-consistency`). Document in `--help`/no-arg flag table print.
- [x] 3.3 In the Orchestration flow of both shells: note (a) detection placement after step 1 (a1); (b) a3 augment `codegraph=on` → advisory call-path confirmation (bounded/non-fatal); (c) a2 clarify `codegraph=on` → advisory callers pre-resolution. Mark all codegraph steps optional + codegraph-gated + non-fatal + bounded.
- [x] 3.4 Add `codegraph` note rows to the Stage → component map of both shells (sra-clarify/sra-augment reference `fragments/codegraph-hint.md` + `fragments/security-dimensions.md` when `codegraph=on`).
- [x] 3.5 MGH_TARGET note: call-path is written into `<change-root>/.mgh-sra/drafts/<cap>.md` (covered by existing subtree guard; `draft_path` absolute verbatim from `prepare_augment` stdout, never interpolated). Confirm `codegraph`/`codegraph_explore` invocations do not hit `block_adhoc_scripts` surfaces (D6) — no hook change.

## 4. Contracts, manifest, report disclosure

- [x] 4.1 `core/contracts/sra/augmentation.md` (also covered by 2.5): `change_context.json` schema **unchanged** (candidate_controls stays textual signal-1). Only the draft gains optional `call_path`.
- [x] 4.2 `sra_manifest.json` producer (orchestrator step 7): `counts` gains `call_path_confirmed` + `call_path_residual` (both 0 when `codegraph=off`).
- [x] 4.3 `sra_manifest.json::boundaries[]`: add codegraph disclosure (assisted? confirmed N / residual M; **not** "全确认"; codegraph static ceiling shrinks-not-zeros mis-wiring; call_path = LLM+codegraph advisory, human review). Keep existing 4 honesty boundaries intact.

## 5. Tests (deterministic regression — R5.8)

- [x] 5.1 Extend `tests/test_distributed_md_purity.py` to cover new/edited shipped MD (edited `sra-augment.md`/`sra-clarify.md`/`sra-consistency.md` stage prompts + both-shell `sra-augment`/`sra-clarify`/`sra-consistency` agent defs + both shell stanzas): assert no dev-meta (R5.x/FDn/Dn/change-folder names/`承/兑现`/`范式锚点`/`本仓`) leaks; `codegraph` as operational external-tool reference is permitted (mirrors init change purity handling).
- [x] 5.2 Add/extend a claude↔opencode parity test (`tests/test_mgh_sra_codegraph_parity.py`): both `mgh-sra.md` shells declare `--no-codegraph`, both run detection after a1, both pass `codegraph=on|off` into a2/a3 tasks, both declare the call-path confirmation optional/codegraph-gated/non-fatal/bounded. Both `sra-augment` agent defs (claude+opencode) declare the same call-path hard constraints.
- [x] 5.3 `tests/test_zero_deps.py`: confirm no new `.py` added (this change adds zero `.py`); AST scan still clean; `codegraph` never imported; `prepare_augment.py`/`merge_augment.py`/`merge_memory.py` contracts unchanged.
- [x] 5.4 If `tests/test_stage_check.py` enumerates stage prompts, confirm edited `sra-*.md` still parse (Sanctioned-tools + draft_path-verbatim + call_path-advisory sections).
- [x] 5.5 Cross-change fragment parity: assert `core/prompts/fragments/codegraph-hint.md` content is identical whether this change or the init change created it (idempotent co-ownership, D8).

## 6. Evaluation (R5.7 TDD-for-docs — prompt change baseline)

- [ ] 6.1 Baseline: on a codegraph-indexed target repo with an mgh-init inventory containing framework-route/AOP controls, run `/mgh-sra` ≥3× with `--no-codegraph`; capture a3 token/round-trip counts + reuse-recommendation mis-wiring rate (variance is the metric).
- [ ] 6.2 Blind A/B: same repo+change, `codegraph=on` vs `--no-codegraph`; assert (a) sra-augment actually issues `codegraph_explore` (not bypass to Read), (b) `call_path_confirmed > 0` for controls genuinely on a request path + `call_path_residual` disclosed for AOP/reflection-only ones, (c) at least one `confirmed:false` case correctly downgrades a file-overlap-only recommendation, (d) **facet coverage**: a data-flow advisory drops ≥1 false sensitive-data gap (field not flowed), a domain-sibling advisory strengthens a signal-2 `reason`, a liveness advisory flags a near-dead recommended control. Record pass-rate/tokens; new failure modes (subagent bypass / call-path false-positive / data-flow false-positive / domain-sibling noise) flow back into stanza wording (D4).
- [ ] 6.3 opencode parity check (resolves O1): confirm opencode subagent context can invoke `codegraph_explore` MCP; if not, confirm CLI `codegraph explore` Bash fallback works (same as init change O1).

## 7. Version bumps, install self-check, validate

- [x] 7.1 Bump version markers in both `mgh-sra.md` shells + affected `core/prompts/stages/sra-*.md` + dual-shell agent def headers (per R5.8).
- [x] 7.2 `install.sh` self-check: confirm edited MD (sra stage prompts + agent defs) mirrors into `.claude/mgh-core/` + `.opencode/`; `codegraph-hint.md` already mirrored (init change) — verify present, do not duplicate. Fail-soft on missing files, CI must fail (R5.8).
- [x] 7.3 `openspec validate improve-mgh-sra-codegraph-enrichment --strict` passes; `openspec status` shows all `applyRequires` artifacts done.

## 8. Deferred — Tier C follow-up change (NOT in scope here)

- [ ] 8.1 (Follow-up) codegraph **seeding** `business_context.json`: auto-produce `interface_authz[]`/`sensitive_fields[]`/`roles[]` initial entries (labeled `source:"codegraph-suggested"`, lower priority than user assertion) on first run, to cut cross-iteration clarification + enrich the `/mgh-blst` consumer. **Out of scope here** because it touches `business_context.json` schema + `merge_memory.py` accumulation semantics + the code-derived-vs-user-asserted priority model (see design Non-Goals + design risk on memory-seeding). This change's a2 clarify only **reduces** questions (pre-resolved facts → no clarification), never **writes** codegraph-derived memory entries.
