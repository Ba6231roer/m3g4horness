# CLAUDE.md — 壳引导(指向 `AGENTS.md`)

> 本文件**不持有任何规则**,只做一件事:让 Claude Code 每个会话把仓库根的
> [`AGENTS.md`](AGENTS.md) 完整载入上下文。
> **研发铁律(R1–R4)与一切操作规范的唯一真相源是 `AGENTS.md`;改规则改那里,
> 不要改本文件、不要把规则内容复制到这里。**

<!-- 以下 @import 在会话启动时把 AGENTS.md 全文内联进上下文 -->
@AGENTS.md

---

## 硬性指令(给 Claude Code)

1. 上述 `@AGENTS.md` 已在会话启动时被内联 = AGENTS.md 全文已在上下文中。
2. **开工前自检**:若上下文里已无 AGENTS.md 全文(长会话被摘要、跨会话续接、
   或本会话尚未触发加载),**必须先用 Read 读取仓库根 `AGENTS.md` 再动手,不得跳过**。
3. 本仓库是安全工具族 `/mgh-*`;R1(上游引用非必要不改)、R2(零运行时依赖,
   新增 pip 须先报备)、R3(文档简练、面向 AI、索引化)、R4(改名后路径规约)违例
   会直接破坏产品特性,优先级高于任何「顺手优化」。具体条文见上方 AGENTS.md。
